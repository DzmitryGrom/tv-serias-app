import React from 'react';
import './index.css'
import  logo from '../../assets/logo.svg'

const Loader = (props) => (
    <div>
        <img
            className='Loader'
            src={logo}
        />
    </div>
)

export default Loader;